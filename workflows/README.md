# Workflow Directory

Put source ComfyUI / RunComfy workflow JSON files here.

Current production workflow:
- `Animate+Wan2.2换风格对口型.json`

Rules:
- Save new workflow JSON files in this directory.
- Do not edit generated runtime copies under `output/**/workflow_canvas.json` or `output/**/workflow_runtime.json` as source files.
- Before running a new workflow on Vast, verify its input filenames, input node IDs, output filename pattern, required custom nodes, and model list.
