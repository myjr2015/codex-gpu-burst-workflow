# 素材资产

这个目录放本地素材，大文件不进 Git。

- `美女图带光伏/`：当前 `001skills` 固定流程的图片来源。新增人物图放这里；如果运行入口没有传 `-ImagePath`，脚本会默认选择这里最新的一张图。
- `原视频/`：原始口播视频素材。
- `美女图无背景纯色/`：旧实验素材，不用于当前 `001skills` 固定流程。

运行时脚本会把选中的图片暂存成 `美女带背景.png`，这是 ComfyUI 工作流里的固定输入名，不要求源文件本身叫这个名字。
