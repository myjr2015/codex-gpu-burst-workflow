# 版本记录

本文件只记录 Git release / tag 级别的版本变化。

运行策略版本、工作流分支、脚本实现版本不要写成 release 版本。详细规则见：

- `docs/版本管理规范.md`
- `config/version-manifest.json`

## 当前状态

- 当前已存在 Git tag：`v1.0.0`、`v1.1.0`、`v1.3.1`
- `main` 当前不是一个已打 tag 的 release
- 下一次整理发布建议使用：`v1.3.10`

## v1.3.9 - 隔离工作树忽略规则

- 新增 `.worktrees/` 到 `.gitignore`，用于后续按 Superpowers subagent-driven development 流程创建隔离 git worktree，避免工作树目录污染版本控制。

## v1.3.8 - LTX2.3 V3 10s 实施计划

- 新增 `docs/superpowers/plans/2026-05-06-ltx23-v3-10s-smoke.md`，把已确认的 LTX2.3 V3 自托管 `10s` 快速探测设计拆成可执行实施计划：新增安全 wrapper、测试 wrapper 默认 prepare-only 和非 `CN` 选机约束、把 action-mimic prepare 脚本纳入 Node 检查、执行本地 prepare-only 验证，并同步 skill / manifest / 版本记录。

## v1.3.7 - LTX2.3 V3 10s 快速探测设计

- 新增 `docs/superpowers/specs/2026-05-06-ltx23-v3-10s-smoke-design.md`，记录 LTX2.3 V3 自托管 `10s` 快速探测设计：从本地 `1124` 个 RunningHub LTX2.3 workflow 中抽取模块，不串巨型 workflow；第一轮用 V3 action-mimic、干净 RGB anchor、真实 10s 中文音频、干净参考动作和 VL 反推背景 prompt，租 Vast `RTX 3090 24GB / 180GB / 非 CN` 低成本验证口型、动作和画面稳定三项硬闸门，必要时再接 RunningHub `204649` 做 mouth-only 口型后处理。

## v1.3.6 - RunningHub 动作口型组合候选记录

- 按用户要求在 Vast 网站临时不稳定时继续使用 RunningHub 测试，未租用 Vast、未创建 CN 实例；最终 `vastai show instances --raw` 确认实例列表为 `[]`。
- `2031596620839657473 / LTX2.3 动作迁移` 在 create 阶段被 RunningHub API 拒绝，原因是云端运行图找不到本地 JSON 里的 `LoadAudio` 节点 `36`，未进入推理、不作为候选。
- 跑通 `2031355634699997185 / LTX2.3人物动作迁移V1`：输出 `512x896`、`24fps`、`265` 帧、约 `11.041667s`，背景干净且无伪字幕，但后半段双手基本回到合拢姿态，动作模仿偏弱，仅作为记录不推荐。
- 跑通 `2031367576470687746 / LTX2.3人物动作迁移V2`：输出 `512x896`、`24fps`、`265` 帧、约 `11.041667s`，比 `203135` 有更明显手势，未见伪字幕/多人/背景文字；随后裁成 `720x1280`、`25fps`、`10s`，接 RunningHub `2046494511848755201 / 精准视频口型同步` 做 mouth-only 后处理，避免 LTX 重画人物和背景。
- 新增当前 RunningHub 动作+口型组合候选：`output/ltx23_talking_head_smoke/ltx23-latentsync204649-action203136-strict25-pad01-20260506-01/deliverables/ltx23-action203136-latentsync204649-v1-10s-originalaudio-720x1280.mp4`，结构为 `720x1280`、`25fps`、`250` 帧、视频 `10.000s`、原音频 `10.008s`；已发布 R2：`https://pub-9bd0a6fd057f4ec9b2938513e07e229a.r2.dev/runcomfy-inputs/ltx23_talking_head_smoke/ltx23-latentsync204649-action203136-strict25-pad01-20260506-01/output/ltx23-action203136-latentsync204649-v1-10s-originalaudio-720x1280.mp4`。
- 自检结果：contact/tail 拼图未见伪字幕、多人或背景文字；动作比 `204071 facehand-04 + 204649` 更明显，但手部仍有局部生成风险，自动 artifact 风险框主要落在红鞋、嘴唇、屋顶边线、手部和椅子区域，需正常播放确认逐字口型和手部自然度后才能算最终验收。
- 更新 `AGENTS.md`、`skills/ltx23_talking_head/SKILL.md` 和 `config/version-manifest.json`，固化当前 RunningHub 临时优先级：需要更强动作时优先看 `203136 + 204649` 组合；需要更稳画面/更少手部风险时保留 `204071 facehand-04 + 204649` 作为口型候选。

## v1.3.5 - RunningHub 动作模板复测记录

- 按用户要求在 Vast 临时不稳定时继续使用 RunningHub 复测动作模板：`2048694979278671873`、`2046642168843997185`、`2039196522113409025` 均跑通并下载结果，但都未超过当前 `204071 facehand-04 + 204649 LatentSync` 组合。
- `204869` 两组测试分别为 `ltx23-rh204869-cleanref-20260506-01` 和 `ltx23-rh204869-facehand-cleanref-20260506-02`：前者有腿脚发散和脸部/构图漂移，后者即使关闭 body DWPose 仍有腿部漂移、脸裁切不稳和小背景标记风险；且该 workflow 没有独立 `LoadAudio`，不能直接作为最终口型路线。
- `204664` job `ltx23-rh204664-videoedit-audio-cleanref-20260506-01` 虽使用真实 `10s.wav` 并产生手势，但人物衣服/身份变成灰色职业装，背景偏离目标光伏场景，尾部出现黑色遮挡物，标记失败不要用。
- `203919` 先把干净参考视频和原音频重编码封装为 `clean_reference_with_10s_audio_reencoded.mp4` 再跑；`facehand` 版动作弱且身体漂移，`body-s045` 版腿脚明显乱动，均不作为推荐候选。
- Vast 只读轮询确认实例列表为 `[]`，没有残留计费；非 `CN` 3090 可搜到约 `$0.16/h` 起的候选，但本轮未创建 Vast 实例。更新 `AGENTS.md`、`skills/ltx23_talking_head/SKILL.md`、`config/version-manifest.json` 固化不要默认复跑这三条 RunningHub 动作模板。

## v1.3.4 - LTX2.3 RunningHub 口型后处理候选记录

- 在 `ltx23-rh204071-audiodrive-facehand-20260506-04` 干净画面基础上追加 RunningHub `2046494511848755201 / 精准视频口型同步` 后处理，避免重新生成整个人物和背景导致光伏背景、文字污染或多人风险回归。
- 新增当前 10s 推荐口型增强交付：`output/ltx23_talking_head_smoke/ltx23-latentsync204649-facehand04-strict25-pad01-20260506-03/deliverables/ltx23-facehand04-latentsync204649-v3-10s-originalaudio-720x1280.mp4`，结构为 `720x1280`、`25fps`、`250` 帧、视频 `10.000s`、原音频 `10.008s`；已发布 R2：`https://pub-9bd0a6fd057f4ec9b2938513e07e229a.r2.dev/runcomfy-inputs/ltx23_talking_head_smoke/ltx23-latentsync204649-facehand04-strict25-pad01-20260506-03/output/ltx23-facehand04-latentsync204649-v3-10s-originalaudio-720x1280.mp4`。
- 复查交付版 contact/tail/artifact 拼图：未见伪字幕、多人或背景文字；自动风险框主要落在嘴唇、红窗框/栏杆、腿鞋、椅子和背景边缘，按人工复核记为误报。补充交付版口型/音频代理指标到 `output/ltx23_talking_head_smoke/_analysis/mouth_audio_comparison_20260506_latentsync204649_deliverable.json`，该指标只作机器信号，最终仍需正常播放验逐字口型。
- 记录对比取舍：`204649` 第一版输出被拉长到约 `10.56s`，不作为最终；`silent_padding_sec=0` 被节点拒绝；`1940753949513433089 / LatentSync 1.6` 结构和视觉相近，但口型代理指标略弱，因此当前优先 `204649-v3 strict25 pad01`。
- 更新 `AGENTS.md`、`skills/ltx23_talking_head/SKILL.md` 和 `config/version-manifest.json`，固化当前口径：RunningHub `204071 facehand-04` 是干净视觉底座，`204649` LatentSync 交付版是当前 10s 口型增强候选；如果播放验口型仍失败，继续在同一干净底座上试视频+音频口型后处理，不再让 LTX 重画整段人物和背景。

## v1.3.3 - LTX2.3 RunningHub 音频驱动候选记录

- 记录 2026-05-06 临时 RunningHub 路线结论：V4 三采 `2044022005221036033` 云端测试在 `512x768` 输出底部伪英文字母，`512x896` 变体 OOM，因此暂不作为推荐动作模仿路线。
- 补充 `2040718940661354497 / LTX 2.3 音频驱动参考角色伪替换工作流` 的多轮测试：`cleanref-01` 和 `stablelegs-02` 腿部发散，`upperonly-03` 可作为全身稳定兜底，`facehand-04` 的节点 `5135` 输出最均衡，`lowmotion-05` 腿脚踢开且墙面出现疑似文字，失败不要用。
- 新增当前 RunningHub 可交付候选记录：`output/ltx23_talking_head_smoke/ltx23-rh204071-audiodrive-facehand-20260506-04/deliverables/ltx23-rh204071-facehand-04-upperbody-reframe-720x1280.mp4`，结构为 `720x1280`、`24fps`、`233` 帧、`9.708333s`、带 AAC 音频；抽帧未见伪字幕/多人，保留脸、手、口型和光伏背景，已发布 R2：`https://pub-9bd0a6fd057f4ec9b2938513e07e229a.r2.dev/runcomfy-inputs/ltx23_talking_head_smoke/ltx23-rh204071-audiodrive-facehand-20260506-04/output/ltx23-rh204071-facehand-04-upperbody-reframe-720x1280.mp4`；仍需正常播放确认逐字口型。
- 更新 `AGENTS.md`、`skills/ltx23_talking_head/SKILL.md` 和 `config/version-manifest.json`，固化当前取舍：RunningHub `204071` facehand 裁切版作为临时最优候选，`upperonly-03` 作为全身稳定备选，V4/低动作版/人物抠像残留黑块参考都不要默认复跑。

## v1.3.2 - LTX2.3 V4 三采候选与 Vast 启动修复

- 接入 `LTX2.3 动作模仿 + 音频对口型` V4 三采 IDLoRA 候选：新增 workflow `workflows/LTX2.3动作模仿+音频对口型-V4三采IDLoRA候选.json`，`stage_ltx23_talking_head_job.ps1` / `run_ltx23_talking_head_smoke_end_to_end.ps1` 支持通过 `-ActionMimicWorkflowSource` 和 `-ActionMimicWorkflowId` 在 V3/V4 动作模仿工作流之间切换。
- 修复 LTX Vast 启动参数和 bootstrap：`launch_ltx23_talking_head_vast_job.ps1` 支持逗号拆分 `ExtraEnv` 且允许 `LTX_UPDATE_COMFYUI=0` 覆盖默认更新；LTX bootstrap 保留 ComfyUI core requirements 以避免缺 `sqlalchemy`，过滤慢速可选 frontend/template/docs 包，并在 V4 runtime 需要时自动下载 `ltx-2.3-spatial-upscaler-x2-1.1.safetensors`。
- 修复 Vast 创建失败检测：`create_vast_instance_minimal.ps1` 现在会把 Vast CLI 输出的 `Failed with error ...` 视为失败，即使 CLI exit code 为 `0`，避免“实例未创建但本地继续执行”的假成功。
- 收紧机器库优先级：`vast_machine_registry.py` 只允许历史成功机器在最低价 `+$0.01/h` 内作为优选，避免老机器压过明显更便宜的可用候选；已补充单元测试。
- 记录 2026-05-06 V4 付费尝试结论：`ltx23-v4-trisample-cleanref-s048-nocn-*` 系列使用 V6 grounded 坐姿 RGB anchor、`光伏10s_clean_reference_v6_skip1.mp4` 干净动作参考、`10s.wav` 音频和 V4 `2044022005221036033`，本地 stage 成功；多轮非 `CN` Vast 3090 候选在 `180GB/120GB/80GB` 下均未能进入推理，最终硬阻塞为 `Your account lacks credit; see the billing page.`，实例列表确认 `[]`，无计费实例残留。V4 目前只能记录为“已接入但未推理验收”，不能替代当前 V6/V3 推荐样本。

## Unreleased

- 新增 KJ 参考视频分段计划辅助入口：`scripts/plan_wan22_kj_reference_segments.py` 可根据污染风险报告生成 seam-aware `segment-plan.json`，`wan22_kj_30s_segmented` stage / runner 支持 `-SegmentPlanPath`，用于后续避开字幕、贴纸、横幅等高风险窗口切分参考视频。
- 收尾清理 Vast 选机口径：移除 profile 中残留的 `default_min_driver_major` 字段，KJ / LTX / 失败经验统一为 `CN` 付费生成硬排除、`TR` 只作为测速和 bootstrap 风险信号、`driver_version` 只记录诊断信息；`scripts/search_cheapest_3090_24g_offers.ps1` 改为默认排除 `CN`，只有显式 `-ExcludeTR` / 旧兼容 `-ExcludeRiskyGeos` 时才同时排除 `TR`。
- 跑通并自检通过当前 `LTX2.3 动作模仿 + 音频对口型` 的推荐 10s 候选：job `ltx23-v6-cleanref-action-s045-nocn-ca-20260505-02` 使用 V6 grounded RGB 坐姿 anchor `ltx23_sitting_rgb_anchor_512x896_bgplate_v6_grounded_synthetic_clean.png` 和干净参考视频 `光伏10s_clean_reference_v6_skip1.mp4`，在 British Columbia RTX 3090 `instance=36191947 / host=207261 / machine=33622` 上跑通并销毁实例。输出 `512x896`、`24fps`、`241` 帧，视频 `10.041667s` / 音频 `10.000000s`，`ActionGuideStrength=0.45`、`ActionLoraStrength=0.65`，prompt 执行 `470.47s`，总耗时到下载 `1242s`；1fps、尾部和风险拼图人工复查未见伪字幕/伪文字、多人，尾部嘴部仍动，R2 已归档：`https://pub-9bd0a6fd057f4ec9b2938513e07e229a.r2.dev/runcomfy-inputs/ltx23_talking_head_smoke/ltx23-v6-cleanref-action-s045-nocn-ca-20260505-02/output/ltx23_talking_head_smoke-ltx23-v6-cleanref-action-s045-nocn-ca-20260505-02_00001_.mp4`。
- 记录 `LTX2.3 clean matte reference` 路线的失败结论：job `ltx23-matte-action-cleanref-nocn-20260505-05` 使用 `mediapipe_tasks_selfie_segmenter` 生成纯灰人物 matte 动作参考并在 Czechia RTX 3090 `instance=36189666` 上跑通，但有效输出 1fps 拼图显示整段底部出现伪中文字幕样式文本；本地字幕遮罩修复会在腿、脚、椅子区域留下明显灰块，因此该方案不作为默认路线。后续继续使用 V6 grounded RGB anchor + 去字幕干净参考视频，或重新制作更强的无文字动作参考。
- 更新 Vast 选机规则：`CN` 作为付费生成硬排除，不进入默认短名单；`TR` 改为测速风险信号，不和 `CN` 一起硬排除；`driver_version` 只记录用于诊断，不再用 `MinDriverMajor=580` 作为硬过滤，真实 CUDA / torch / bootstrap 日志报错后再止损切机。新增 `skills/vast-machine-selection/SKILL.md` 作为付费选机、测速、价格和切机止损的统一入口，并同步默认 Vast search query 为 `geolocation notin [CN]`。
- 补充 `LTX2.3 动作模仿 + 音频对口型` 的 V6 坐姿全身干净参考样本：job `ltx23-v6-grounded-cleanref-fullbody-20260505-01` 使用 grounded RGB 坐姿 anchor `ltx23_sitting_rgb_anchor_512x896_bgplate_v6_grounded_synthetic_clean.png` 和去字幕参考视频 `光伏10s_clean_reference_v6_skip1.mp4`，在 Kentucky RTX 3090 `instance=36179630 / host=436128 / machine=57621` 上跑通并销毁实例。输出 `512x896`、`24fps`、`241` 帧，视频 `10.041667s` / 音频 `10.000000s`，prompt 执行 `263.22s`，总耗时到下载 `1286s`；1fps 和尾部拼图未见字幕/伪文字、未见双人，凳脚和阴影已读作接触地面。该样本作为当前“坐姿全身 + 无字幕 + 动作模仿/口型候选”的视觉基线，动作强度因清理参考视频而保守，仍需用户正常播放确认口型和手势强度。
- 跑通 `LTX2.3 动作模仿 + 音频对口型` 的首个自托管候选：基于 RunningHub `2044017351640748034`（`LTX2.3_自定义音频+人物动作迁移V3（单采IDLoRA）`）新增 `workflows/LTX2.3动作模仿+音频对口型-V3候选.json` 和 `scripts/prepare_ltx23_action_mimic_prompt.mjs`，stage / bootstrap / remote submit 支持 `-ActionMimic`、参考视频、DWPose、Union Control IC-LoRA、ID LoRA 和 `LTXVReferenceAudio`。付费 job `ltx23-action-mimic-v3-20260505-01` 在 Czechia RTX 3090 `instance=36137864 / host=151822 / machine=81287` 上跑通并销毁实例，输出 `512x896`、`24fps`、`241` 帧，视频 `10.041667s` / 音频 `10.000000s`，prompt 执行 `284.11s`；节点校验确认 `DWPreprocessor`、`LTXAddVideoICLoRAGuide`、`LTXICLoRALoaderModelOnly`、`LTXVReferenceAudio` 等动作+口型核心节点存在。1fps、尾部和参考对比拼图未见伪字幕、多人或双人首帧问题，尾部嘴部仍动，手势已明显跟随参考视频节奏；R2 已归档，后续需用户正常播放确认口型后再跑 30s/60s。
- 记录 `LTX2.3 + 透明人物图 + 背景提示词 + VBVR 0.60` 付费样片结论：`ltx23-vlbg-transparent-vbvr-20260505-03` 在 Spain RTX 3090 上跑通并已销毁实例，输出 `512x896`、`24fps`、`241` 帧，视频 `10.041667s` / 音频 `10.000000s`，prompt 执行 `263.44s`，R2 已归档；但 1fps 和尾部拼图明确出现伪英文字幕碎片，且 VBVR `0.60` 在该配置下没有产生有效手势动作，因此直接把透明 RGBA 人物图交给 LTX2.3 按背景 prompt 补全场景的方案被否决。后续路线改为先用透明人物图合成无文字 RGB 光伏背景 anchor，再送入现有无字幕 LTX2.3 + VBVR 链路；动作部分需要等无字幕和全身构图稳定后再接 IC-LoRA / reference-motion。
- 接入 `LTX2.3 候选新路线` 的 prompt-only 背景提示词模块：`scripts/prepare_ltx23_talking_head_prompt.mjs` 现在把正向提示词拆成 `speaker_prompt`、`background_prompt`、`camera_prompt`、`prompt_guardrails` 组合，并保留 `-PositivePrompt` 作为完整覆盖；`scripts/stage_ltx23_talking_head_job.ps1` 和 `scripts/run_ltx23_talking_head_smoke_end_to_end.ps1` 新增 `-BackgroundPrompt` 等参数，manifest/metadata 会记录最终 prompt 和背景片段。已本地 stage-only 验证 `_test-ltx23-bgprompt-prepare`：`512x896`、`24fps`、`241` 帧、VBVR strength `0.60`，未启动付费 Vast。该模块不加载 PromptRelay / Qwen3-VL / Gemini 节点，也不下载额外提示词大模型。
- 固化 `LTX2.3 + VBVR` 动作增强候选：`scripts/prepare_ltx23_talking_head_prompt.mjs` / stage / end-to-end 脚本支持第二个 motion LoRA，bootstrap 可按需下载 `Ltx2.3-Licon-VBVR-I2V-240K-R32.safetensors`，并将链路接成 `distilled LoRA -> VBVR motion LoRA -> LTX2_NAG -> sampler`。已验证 `ltx23-vbvr-motion-s06-20260504-01`：VBVR strength `0.60`，输出 `512x896`、`24fps`、视频 `10.041667s` / 音频 `10.000000s`，中段右手有明显胸前小手势，抽帧未见伪字幕/伪文字，尾部仍有嘴部动作，R2 已归档，Vast 实例已销毁。同时修复旧 CPU 3090 机器上 `kornia_rs` 导致 ComfyUI `Illegal instruction` 的问题：LTX2.3 bootstrap 现在跳过浮动 `kornia` requirement，显式 pin `kornia==0.7.1` 并卸载 `kornia_rs`。
- 新增并推进 `LTX2.3 候选新路线` 的自托管无字幕底座：建立 `ltx23_talking_head_smoke` profile / `skills/ltx23_talking_head/SKILL.md` / Vast 3090 自动化脚本，下载并分析 RunningHub `ltx2.3`、`ltx 2.3` workflow 共 `1124` 个，保留全量分析到 `workflows/temp/runninghub_ltx23_exact/`。已验证 `2043593704170070018` 自托管链路能生成 `512x896`、约 `10.04s` 视频，但两次 smoke 均出现伪字幕/伪文字：首条有大面积中文伪字幕，第二条强负向词 no-NAG 补交把问题降成英文伪字贴片但仍不可生产；两台 Vast 实例均已销毁。当前默认候选切到 `2040333916862685186` 的“单图+音频对口型无字幕”工作流，本地 prepare 已能折叠 RunningHub 辅助节点、用 `CreateVideo + SaveVideo` 替代 `VHS_VideoCombine`、裁剪音频到目标时长、保留 `LTX2_NAG`，并在 Vast job `ltx23-nosub-nag-20260504-01` 上跑通：输出 `512x896`、`24fps`、视频 `10.042s` / 音频 `10.000s`，prompt 执行 `125.28s`，抽帧和尾部 4fps 拼图未见伪字幕/伪文字，尾部仍有嘴部动作，实例 `36102434` 已销毁且实例列表为空；后续需用户人工确认口型后再接 VBVR / IC-LoRA / 背景提示词模块。
- 归档 2026-05-04 视频二创路线阶段结论：更新 `skills/history_video_pipeline_skills/SKILL.md`、`skills/wan22_kj_30s/SKILL.md` 和 `skills/badskills/SKILL.md`，明确当前项目暂停状态、`KJ 3.0 480p竖屏同图锚定版` 的可用边界、Wan2.2/KJ 在 `720x1280` 上过重而 `480x848` 清晰度不足、`MultiTalk / InfiniteTalk` 10s smoke 因最后几秒不说话和口型不对被否决，以及后续 `LTX2.3` 必须作为独立 profile / skill 重新评估。
- 修正 `KJ 3.0 480p竖屏同图锚定版` 的 60s 验片状态：此前 `kj60-kj3p0-480p-4090-reuse-20260503-01` 只按快速抽帧记为自检通过；用户复查发现 `28s-31s` 有多手/手部结构异常，`43s-45s` 有短时闪屏，因此该 60s 样本不能作为验收通过样本。补充诊断：`28s-31s` 横跨 30s 分段接缝，且参考视频同窗口有字幕、定位气泡、红勾和贴纸覆盖身体/手部区域；`43s-45s` 参考视频有大红黑文字覆盖躯干/手部区域。这类问题属于结构/时序生成失败，不是红点色块，不能只靠本地 OpenCV inpaint 无损修复。
- 记录 `KJ 3.0 480p竖屏同图锚定版` 的 `60s=30s+30s` 复用实例验证：job `kj60-kj3p0-480p-4090-reuse-20260503-01` 在 Michigan RTX 4090 `machine_id=56169 / host_id=65203` 上直接复用已安装环境推理，输出 `59.648s`、`480x848`、`16fps`、带音频；两段推理墙钟约 `17m36s + 16m02s`，本地 `ffmpeg concat_copy` 合并并发布 R2。快速抽帧自检曾未发现明显问题，但该判断后来被用户精看片段推翻，以上一条修正记录为准。
- 修复 `scripts/validate_wan22_kj_30s_runtime.mjs` 的 KJ 3.0 校验口径：校验脚本现在和 prepare 脚本一致，把所有竖屏输出识别为稳定竖屏策略，允许 `480x848` 默认 `context_frames=121/context_overlap=16`，避免本地 `PrepareOnly` 阶段误报“expected 241, got 121”。
- 命名并记录 `KJ 3.0 480p竖屏同图锚定版`：这是 `wan22_kj_30s_segmented` 的方案版本，不是 Git release，也不是新工作流；内部追踪名 `KJ3.0-480p-portrait-anchor`。该版本固定使用 `480x848`、稳定竖屏 context `121/16`、`offload_img_emb/offload_txt_emb=true`、DockerHub v3 template，并复用同图锚定规则；`kj10-4090-480x848-anchor-20260503-try1` 的 `10s` 成片已由用户验收合格，后续 `30s/60s` 仍需生成后验片。
- 修复 KJ `480x848` 竖屏默认上下文策略：`scripts/prepare_wan22_kj_30s_prompt.mjs` 现在把所有竖屏输出都归入稳定竖屏策略，默认使用 `context_frames=121`、`context_overlap=16`，并开启 `offload_img_emb/offload_txt_emb`。同步记录 RTX 4090 job `kj10-4090-480x848-anchor-20260503-try1`：HF `93.32 MiB/s`，模型下载约 `5m11s`，采样 `6/6` 约 `5m57s`，prompt 执行 `400.00s`，输出 `9.836s`、`480x848`、`16fps`、带音频；实例按用户要求保留未销毁。
- 修复 KJ 竖屏 10s 质量测试的上下文参数：`scripts/prepare_wan22_kj_30s_prompt.mjs` 现在按输出分辨率选择默认 context，`720x1280` 等高像素竖屏保持生产稳定参数 `context_frames=121`、`context_overlap=16`，并强制 `offload_img_emb/offload_txt_emb=true`；`720x720` 及以下才保留更长 context 默认。同步更新 runtime 校验，避免把 `context_frames=41/context_overlap=8` 速度测试误当画质验收。已用 RTX 4090 job `kj10-4090-repro-20260503-121023` 验证 10s 输出：顶部背景闪烁指标 `1.085`，接近稳定 4090 30s 样本 `1.024`，明显优于此前 `cf41` 闪烁样本 `5.767+`。
- 将 KJ 2.0 单段和分段脚本默认输出改为抖音 9:16 竖屏 `720x1280`：stage 阶段先用 FFmpeg 把方形 IP/同图锚定图转成 `720x1280` 画布，背景使用同图放大模糊铺满、前景等比居中保留；runtime JSON 同步设置 `VHS_LoadVideo.custom_width=720/custom_height=1280`，并让 `LayerUtility: ImageScaleByAspectRatio V2` 按宽度 720 缩放锚定图。已本地 PrepareOnly 验证 `wan22_kj_30s` 和 `wan22_kj_30s_segmented` 都能生成 `720x1280` staged 输入与 runtime。
- 记录 KJ 60s 4090/3090 DockerHub v3 同 seed 对比中的 48-51s 手部异常复盘：两次运行使用相同 seed、prompt、negative prompt、`ip_image.png` 和 `reference_segment_02.mp4`，但输出不同；4090 版本在 48-51s 被用户发现多手/手部异常，3090 版本未明显放大同类问题。对应参考窗口含字幕、定位 pin/红色标记和大白色横幅，归类为“参考视频横幅/符号污染动作条件 + 生成不确定性”，不是单纯 GPU 品牌问题；后续遇到手部结构异常应先清理或避开污染参考窗口，再重跑受影响的 30s 分段。
- 整理 Vast “我的模版”：保留 DockerHub v3 当前稳定 4090 模板 `400607 / eb3ff9185d9de9a9482c2cffbdfd8f9f`，新增同配置 RTX 3090 模板 `401196 / ffe5b8f532b7b14b825df78c43be59d9`，并删除旧 GHCR/v1/v2/v3 smoke 实验模板 `399965`、`399966`、`399967`、`399969`、`399970`、`400059`、`400060`、`400370`；复查 Vast 私有模板只剩 4090/3090 两个 DockerHub v3 模板，当前实例列表为 `[]`。
- 记录 KJ v3 registry/HF 选机测速对比：在同一台 Michigan RTX 4090 `machine_id=56169 / host_id=65203`、`DiskGb=240` 上只做 onstart 级测速，不跑推理、不跑 ComfyUI bootstrap、不下载 32.55GiB 全量模型。DockerHub v3 实例 `35993311` 约 `50.4s` 到 running，HF `104.00 MiB/s`，DockerHub layer `101.88 MiB/s`，GHCR layer `33.56 MiB/s`；GHCR v3 实例 `35993402` 本地 token manifest 预检为 `200`，不是 401 权限问题，但 Vast image pull 超过 stop-loss 窗口仍未到 onstart；base 实例 `35993588` 约 `57.7s` 到 running，HF `103.81 MiB/s`，DockerHub layer `102.45 MiB/s`，GHCR layer `32.50 MiB/s`。结论：当前优先 DockerHub v3，GHCR v3 暂不作为默认，base 只作 DockerHub v3 失败时的兜底；收尾确认 `vastai show instances --raw` 为 `[]`。
- 修正 KJ v3 同机镜像对比结论口径：同机第二次运行命中镜像层缓存后变快不能作为 v3 更快的证据，缓存样本只说明复跑会快。当前可成立的结论改为：v3 在 onstart 后的环境准备阶段明显更短，`remote.bootstrap` 从 base 的 `210s` 降到 `23s`；但第一次使用 v3 必须把 DockerHub 镜像拉取时间算进去，本次首次 v3 到 `validate_nodes` 约 `4m12s`，base 成功样本约 `4m37s`，只能说略快且主要价值是依赖确定性，不能说总冷启动稳定大幅领先。
- 记录同一台 Vast 4090 上的 KJ v3 镜像与基础镜像 `validate_nodes` A/B 对比：测试机为 Michigan `machine_id=56169 / host_id=65203`，`RemoteStopAfter=validate_nodes`，不提交推理且跳过模型下载。三条有效样本 HF 均约 `103 MiB/s`；基础镜像成功样本 `kj-ab-base-r2-20260502-1034` 的 `remote.bootstrap=210s`、`python_dependencies=190s`，而 DockerHub v3 样本 `kj-ab-v3-r1-20260502-1024` / `kj-ab-v3-r2-20260502-1041` 均为 `remote.bootstrap=23s`、`python_dependencies=23s`、`custom_nodes=0s`。结论：v3 的价值是固化 custom nodes / Python / torch / ONNX 环境，不能替代 32.55GiB 大模型下载；Docker/container 冷启动仍需单独计时。
- 记录 `KJ 2.0 环境镜像模板版` v3 的 2026-05-02 不推理冷启动对比：使用 `RemoteStopAfter=bootstrap` 验证 DockerHub v3 + Vast template 在 3090/4090 上的推理前耗时。成功样本显示 custom nodes / Python / ONNX 阶段约 `37-41s`，模型下载仍是主耗时；3090 Bulgaria 样本 HF `123.45 MiB/s`、模型下载 `153s`、remote bootstrap `190s`，4090 Estonia/Hungary 样本 remote bootstrap 分别 `360s` / `275s`。同时记录多台机器卡在镜像/容器启动前或 DNS/docker login/label lookup 阶段，说明 v3 镜像不能只看 GPU 型号，必须把 Docker/container 启动成功率和 HF 大文件下载作为独立指标。
- 修复 `summarize_vast_job_timing.ps1 -FetchLog` 的 Windows 日志抓取编码问题：调用 `vastai logs` 时强制 `PYTHONUTF8=1`、`PYTHONIOENCODING=utf-8` 和 UTF-8 console output，避免远端日志包含特殊字符时被本地 GBK 中断。
- 补充 `skills/wan22_kj_30s/SKILL.md` 的 KJ v3 环境镜像测速口径：`validate_nodes` 当前会跳过模型下载，只能算环境-only smoke；要做“不跑推理但包含模型冷下载”的推理前冷启动比较，应使用 `RemoteStopAfter=bootstrap`，并分开记录 Docker/container、HF speedtest、依赖/ONNX 和真实模型下载耗时。
- 修复 `KJ 2.0 环境镜像模板版` v3 实测链路：确认 GHCR v3 因当前本地 GitHub token 缺少 `read:packages` 导致 manifest 检查 `401`、Vast 卡在 pre-container pull；将默认实验镜像改为 DockerHub `j1c2k3/codex-wan22-kj-comfy:cuda129-py312-kj-v3`，创建 Vast template `eb3ff9185d9de9a9482c2cffbdfd8f9f`。同时修复 staged ONNX smoke 的 `output_path` 未从 `sys.argv[1]` 读取导致的 `NameError`，并在 California RTX 4090 `machine_id=17049 / host_id=96607` 上验证 `RemoteStopAfter=onnx_cuda` 通过：HF `67.41 MiB/s`，ONNXRuntime `CUDAExecutionProvider` tiny session 成功，实例已销毁。
- 修复 `KJ 2.0 环境镜像模板版` v2 的 ONNXRuntime CUDA 缺陷并升级默认镜像 tag 到 `cuda129-py312-kj-v3`：镜像构建和远端 bootstrap 改为安装 `onnxruntime-gpu[cuda,cudnn]>=1.21.0`，补齐 Python NVIDIA CUDA12 runtime 库路径，强制验证 `CUDAExecutionProvider`、`libcublasLt.so.12` / `libcudnn.so.9` / `libcudart.so.12`，并用 tiny ONNX Identity 模型创建 CUDA session 跑一次推理；新增 `RemoteStopAfter=onnx_cuda`，可在下载大模型和提交推理前低成本拦截“退回 CPU 前处理”的坏镜像。
- 记录 RTX 3090 Vast 选机价格规则：KJ / Wan2.2 付费任务默认只选全部费用合计低于 `$0.20/h` 的 3090，必须按完整工作流默认 `DiskGb=240` / `--storage 240` 后的 `dph_total` 判断，不能只看裸 GPU 价格；HF 纯测速可以用小磁盘快速测网络，但不能当最终生产成本。
- 修复本地 `api.txt` 的 DockerHub 凭据兜底解析：兼容 `DockerHub / 用户名:j1c2k3 / dckr_pat...` 三行格式，也兼容 `DockerHub / j1c2k3 / dckr_pat...` 简写格式，避免 Vast 创建实例时把用户名行误当成 token 导致 `docker login failed`。
- 优化 `KJ 2.0 环境镜像模板版` 启动路径：KJ 模型冷下载新增 `KJ_MODEL_DOWNLOAD_PARALLELISM`，默认 3 路并行、最多 4 路，每个模型先下载到 `.part` 临时文件再原子移动，任一模型失败会写 fail marker 并让 bootstrap 整体失败；torch 检查改为以 `torch + CUDA>=12.4 + GPU 可用` 为核心，缺 `torchvision` / `torchaudio` 时只按当前 torch 版本补装辅助包，不再因为辅助包缺失触发整套 torch force reinstall；环境镜像默认 tag 升为 `cuda129-py312-kj-v2`，镜像构建时记录 torch 兼容状态；GHCR v2 镜像 Actions 构建成功后已创建 Vast template `3f38ca38792bcefce25bb1688f4ca2ca`。
- 修复 KJ v2 template smoke 暴露的 torch 辅助包补装问题：当基础镜像自带 `torch=2.11.0+cu130` 一类更高 CUDA wheel 时，`torchvision` / `torchaudio` 补装不再固定走 cu124，而是按当前 `torch.version.cuda` 选择对应 PyTorch wheel index；如果上游没有匹配的辅助包 wheel，默认警告后继续，不再因为非核心 `torchaudio` 缺失阻断 template 验证。
- 优化 KJ 模型冷下载稳定性：bootstrap 的 curl 模型下载增加 `-C -` 断点续传，配合 `.part` 临时文件和 retry，降低 16GiB 主模型遇到 HuggingFace 连接重置时从头重拉的概率。
- 验证 KJ v2 template smoke：`kj30s-template-v2-smoke-fix-20260501-164926` 在 California RTX 4090 `machine_id=17049 / host_id=96607` 上通过 `RemoteStopAfter=validate_nodes`，HF speed gate `100.29 MiB/s`，冷模型下载 3 路并行完成，KJ custom nodes 从镜像复用，torch 栈复用且未整套重装，ComfyUI API 和 `object_info` 节点校验通过，日志确认停止在 `/prompt` 前；收尾复查 Vast 实例列表为空。
- 修复 KJ launch 脚本创建实例后轮询 `vastai show instances` 未加载本地 Vast key 的问题；否则实例已创建并开始计费，但本地会因 `403` 非 JSON 输出中断。
- 增加 Vast 实例创建阶段的私有 registry 登录支持：`create_vast_instance_minimal.ps1` 和 KJ launch 脚本支持 `-PrivateRegistryLogin -RegistryHost ghcr.io -RegistryUsername myjr2015`，token 从本地 GitHub 凭据读取并在输出中脱敏；修正“把完整 login 写入 template 即可拉私有 GHCR”的错误假设。
- Vast template helper 在执行 `vastai create template` 时改为捕获输出并对 DockerHub/GHCR token 脱敏，避免创建私有 registry template 时命令返回内容意外带出密钥。
- 修复 KJ 环境镜像 GitHub Actions workflow 的 YAML 描述字段引号问题，避免 `registry` 输入说明里的冒号导致 workflow 在 push 阶段直接无 job 失败。
- 增加 KJ 环境镜像 template smoke 停止点：`remote_submit_wan22_kj_30s.sh` 支持 `KJ_REMOTE_STOP_AFTER=hf_speedtest|bootstrap|wait_api|validate_nodes`，`launch_wan22_kj_30s_vast_job.ps1` 暴露 `-RemoteStopAfter`，可在 HF 测速、bootstrap、ComfyUI 启动和节点校验后停止，避免为了验证 template 启动速度而提交完整 30s 推理。
- 将 `KJ 2.0 环境镜像模板版` 默认镜像仓库从 DockerHub 切到 GHCR：GitHub Actions 现在支持 `registry=ghcr|dockerhub`，默认推送 `ghcr.io/myjr2015/codex-wan22-kj-comfy:cuda129-py312-kj-v1`，避免本地只有 DockerHub token、缺少 DockerHub username 时卡住；Vast template helper 同步支持通用私有 registry login，但默认不把 token 写入命令行。
- 新增 `KJ 2.0 环境镜像模板版` 实验路径：增加 `docker/wan22-kj-comfy-env/Dockerfile`、GitHub Actions 构建 workflow、DockerHub/GitHub secret helper、Vast template helper 和 `1.2-docker-env-template` runtime；该路径只预装 KJ custom nodes / Python 依赖，不包含模型权重、不使用 Vast volume，默认生产入口不变。
- 记录用户验收结论：KJ 成片自动精修 `v5` 当前可接受，`polished-auto-v5.mp4` 暂定为 `kj60-b11-sameframe-30x2-20260501` 的推荐精修输出；后续同类 KJ 2.0 同图锚定版成片默认走 `生成原片 -> v5 本地精修 -> before/after 人工确认 -> 发布/归档`，继续禁止把大 halo 粗修设为默认。
- 增加 KJ 成片自动精修 v5 智能模式：`scripts/polish_generated_artifacts.py` 默认从单红色扩展为 `red/yellow/green/magenta` 多颜色候选，保留 `cyan/blue` 显式参数可开但默认关闭以避免误伤天空和光伏板；对已确认目标增加银白高光/细线残留候选 mask，尝试清理红球之外的同一污染物边缘。已验证 `polished-auto-v5.mp4`：全片用时 `424.8s`，输出 `59.625s`、`720x720`、`16fps`、音频 `59.603696s`；当前样本实际安全修复 `5` 帧、`5` 个红色组件，跳过 `911` 个常驻/皮肤风险组件。v5 仍以不误伤人物为优先，不把大范围 halo 粗修作为默认，因为 `48px` halo 小样本会把腿边和光伏背景补成明显蓝灰块。
- 修复 KJ 成片自动精修脚本对不足 1 秒红色污染的边缘漏帧问题：`scripts/polish_generated_artifacts.py` 新增 `--target-frame-padding`，默认每个检测目标前后补 `2` 帧，但仍只修匹配的红色目标并保留皮肤保护。已重新验证 `polished-auto-v4.mp4`：全片用时 `275.5s`，触达 `5` 帧、修复 `5` 个红色组件、跳过 `911` 个组件，29.500s-29.750s 红色挂件全部清除，28.562s / 29.812s 手部控制帧未被误修。
- 新增 KJ 成片自动精修一条龙脚本 `scripts/polish_generated_artifacts.py`：对生成后视频执行异常前检、定位短时红色候选、OpenCV 小范围 inpaint、FFmpeg 重封装音频、异常复检和 before/after 拼图输出；明确该方案不是纯 FFmpeg，只适合孤立小红点/贴纸色块，不用于手、脸、身体结构问题。已在 `kj60-b11-sameframe-30x2-20260501` 上验证 v3：全片用时 `279.4s`，触达 `4` 帧、修复 `4` 个红色组件、跳过 `702` 个组件，输出 `polished-auto-v3.mp4`，并通过 28.5-30.0s 定点对比确认红色挂件被修掉且手部误修已避免。
- 增加 KJ 2.0 方案的友好命名规则并写入 `AGENTS.md`：对外统一称 `KJ 2.0 同图锚定版`，括号里保留内部追踪名 `B1.1 same-frame anchor`；失败实验统一称 `KJ 2.0 背景/Mask失败版`，括号里保留 `B2 bg_images/mask`，避免后续沟通继续使用难记的纯英文内部编号。
- 记录 KJ `2.0 B1.1 same-frame anchor` 验证通过：`kj60-b11-sameframe-30x2-20260501` 用同一张完整人物+光伏背景 anchor 图作为每个 30s 分段的 `ip_image.png`，不接 `WanVideoAnimateEmbeds.bg_images` / `mask`，合并输出 `59.648s`、`720x720`、`16fps`、带音频；动作指标恢复到 `body mean 8.254`、`mouth mean 10.239`、`background mean 0.366`，人工复查关键帧、28-33s 接缝、26-30s、48-52s 和异常扫描拼图未见双头/多人/明显手部结构崩坏，当前作为 2.0 固定场景可用方案。
- 纠正 KJ `2.0 B2 bg_images/mask` 结论：`kj60-b2-bgmask-20260501-0100` 不能算通过；详细对比发现 `bg_images` + `mask` 接入压制了原 KJ 动作/表情条件，且 `bg_image.png` 实际包含完整人物，导致嘴部和身体运动几乎锁死，指标仅 `body mean 1.2`、`mouth mean 0.705`。后续不要继续使用 B2 结构，除非重新设计 mask 语义并小样本验证动作不被压制。
- 为 `wan22_kj_30s_segmented` 增加参考视频污染预检闸门：`scripts/run_wan22_kj_30s_segmented_end_to_end.ps1` 支持 `-ReferenceRiskPolicy Warn|FailOnHigh|Off`，默认只报警；2.1/2.2 验证可用 `FailOnHigh` 在 Vast 选机、上传和付费推理前拦住高风险参考视频。已用 `_test-risk-gate-20260501` 验证当前 `光伏60s.mp4` 会因 1 个高风险窗口、max score `11.374` 被阻断。
- 记录 KJ `2.1-next` 本地样本闸门结论：`光伏60s.mp4` 的 OCR glyph mask + OpenCV inpaint、加粗 Telea/NS、局部变体和 `simple-lama-inpainting` 单帧模型级样本均未通过；文字/气泡虽可被部分抹除，但会残留白/橙/灰块，或把身体、手、腿、衣服和光伏板补成错位半透明区域，因此不跑全片、不进入 Vast，实例 `35889784` 保留不销毁。
- 实现 2.1 通用参考视频污染本地预处理入口 `scripts/clean_reference_overlay_windows.py`：可读取 `analyze_reference_overlay_risk.py` 的 `cleanup_plan`，按风险窗口输出清理版参考视频、`cleaning-report.json`、before/after 拼图，并支持本地小样本时间段验证。
- 扩展 `scripts/analyze_reference_overlay_risk.py` 输出 `cleanup_candidates` / `cleanup_plan`，为红点、定位 pin、字幕、贴纸、横幅等污染源提供机器可读候选框、建议动作和 `needs_review` 安全级别。
- 记录 2.1 本地轻量规则法闸门结果：`光伏60s.mp4` 的 conservative_v9 全片本地清理仍未通过，before/after 拼图可见近人物文字灰块、气泡/中段大字残留；清理后复扫仍为 `0.0s-59.8s` 高风险窗口，max score `8.065`，因此禁止进入 Vast 付费推理。
- 记录 B2 背景锚定复核修正结论：`kj60-b2-bgmask-20260501-0100` 合并视频虽为 `59.648s`、`720x720`、`16fps`、带音频，且快速复核未见双头/多人，但后续发现嘴巴和身体几乎不动，因此视觉结论从 `pass` 修正为 `failed_motion_suppressed`。
- 为 `wan22_kj_30s_segmented` 增加 2.0 B2 背景锚定 runtime：纯色透明人物图仍作为 `ip_image.png`，单张提示词生成背景作为 `bg_image.png`，通过 `RepeatImageBatch(frame_load_cap)` 接入 `WanVideoAnimateEmbeds.bg_images`，并用 `ip_image.png` alpha 经 `InvertMask/GrowMask` 后接入 `WanVideoAnimateEmbeds.mask`，避免把成片抽帧误当人物 IP。
- 扩展 KJ 分段 stage / runner / onstart / runtime 校验：支持 `-BackgroundImagePath`、`-BackgroundMaskGrow`，上传 `bg_image.png`，校验 B2 节点接线，并在远端 `object_info` 中检查 `RepeatImageBatch`、`LoadImageMask`、`InvertMask`、`GrowMask`。
- 补充 2.1 通用参考视频清理实现清单：优先从现有污染扫描脚本产出机器可读 mask 草案，新增清理预处理入口，对红点、字幕、贴纸、横幅分级处理，并在进入 Vast 推理前执行清理质量闸门。
- 新增 `docs/KJ参考视频清理方案TODO.md`：记录 KJ 参考视频污染清理路线，明确 2.0 继续使用规则检测、小范围局部清理、复跑受影响分段；2.1 评估 ProPainter / E2FGVI 视频级 inpainting；2.2 评估 LaMa / MAT / Crop & Stitch / SAM / OCR 等图像级或局部修复方案，暂不把这些插件写入生产 Vast bootstrap。
- 新增 KJ 参考视频污染体检脚本 `scripts/analyze_reference_overlay_risk.py`：在付费生成前抽帧扫描参考视频中的红色对勾、定位点、贴纸、横幅、字幕和高饱和彩色图标，输出 JSON/CSV/审查拼图，用于提前标出可能污染动作条件的时间窗口。
- 新增 KJ 成片异常复查脚本 `scripts/analyze_generated_artifact_risk.py`：生成后抽帧扫描短时红点、贴纸残留、彩色小块等候选异常，输出复查窗口和拼图；该脚本只作为候选清单，不能替代人工逐帧验收。
- 记录 `wan22_kj_30s_segmented` 的通用质量风险：参考视频贴图/字幕污染具有不确定性，同一类污染源不一定每次都显性复现，可能表现为多手、额外手势、手部漂移、红点、贴纸残留或局部肢体变形；后续流程必须同时执行“生成前参考视频污染体检”和“生成后成片异常复查”，不能只靠提示词或固定 seed。
- 记录 `wan22_kj_30s_segmented` 新失败模式：参考视频里的贴图文字、字幕或横幅即使没有被直接重绘到成片，也会污染动作/姿态条件，导致对应时间段出现手部漂移、多手、额外手势等动作模拟问题；后续长视频分段生成前应先清洗参考视频的文字遮挡区域，再切段上 Vast。
- 修正 `wan22_kj_30s` / `wan22_kj_30s_segmented` 默认人物 IP：当前光伏参考视频是坐姿，默认图从 `素材资产/美女图无背景纯色/纯色站着.png` 改为 `素材资产/美女图无背景纯色/纯色坐着.png`；分段流程保留固定 seed 和单主体负面词，但不再硬编码“椅子”等具体道具，场景和道具应由视频反推提示词决定。
- 纠正 `wan22_kj_30s_segmented` 的 60s 两段验证结论：任务 `kj30s-seg60-4090-nl-20260430-133517` 首版第二段出现站立人物残影；复用同一实例重跑 `s02fix` 后虽然去掉了第二个人，但第一段和第二段椅子状态不一致，因此视觉验收仍为失败，不能作为合格 60s 方案。
- 修复 `wan22_kj_30s_segmented` 下载兜底：当 ComfyUI 重启导致早段 `/history` 记录丢失时，下载脚本会根据远端 `segment_xx end` 日志和确定性输出前缀推断 MP4 文件名，并扩大日志检查窗口，避免已生成文件无法自动下载。
- 新增 `wan22_kj_30s_segmented` 实验入口：自动按输入视频时长切成 `30s` 分段，在同一台 Vast 实例内顺序复用 KJ 30s workflow 生成各段，下载后用本地 `ffmpeg concat` 合并；该方案不新增 ComfyUI 合并插件，也不声称解决跨段 `continue_motion`，需要 60s 付费验证后再判断质量。
- 验证 `wan22_kj_30s` 的 RTX 4090 对比路径：任务 `kj30s-4090-greece-20260430-121150` 在 Greece `machine_id=50910 / host_id=283684` 上成功生成 `29.8125s`、`720x720`、`16fps`、带音频视频；HF 实测速率 `89.04 MiB/s`，模型下载 `474s`，Comfy prompt 执行 `00:36:13`，总耗时到本地下载 `2995s`，已上传 R2 并更新机器库。
- 为 `wan22_kj_30s` 增加 HuggingFace 下载速度前置闸门：冷启动前按 11 个配置模型文件合计 `32.55 GiB` 估算下载时间，默认要求实测速度不低于 `15 MiB/s` 且预计模型下载不超过 `30 min`，不合格则在 bootstrap 前停止并销毁实例。
- 新增 `scripts/select_wan22_kj_30s_offer_by_hf_speed.ps1`：先列出 `10-20` 个 4090 候选，按价格、驱动、地区和黑名单过滤；每轮只短租测试 `1-3` 台 HuggingFace 速度，`5 min` 内未进入 HF 测速日志的机器直接判为启动慢并销毁，不合格自动进入下一批，避免逐台全量烧钱。
- 修复 `wan22_kj_30s` launch 阶段 HF 环境变量传参：`ExtraEnv` 改为逗号合并字符串传入，避免 PowerShell 因重复参数或数组绑定导致 Vast 实例未创建。
- 新增 `wan22_kj_30s` 工作流分支：使用 `素材资产/美女图无背景纯色/` 里的姿态匹配人物 IP 图，使用 `素材资产/原视频/光伏30s.mp4` 作为 30s 参考动作/表情视频，通过 KJ 版 Wan2.2 Animate workflow 根据提示词重绘光伏背景。
- 验证 `wan22_kj_30s` 的 2.0 单段直出路径：任务 `kj30s-2p0-20260430-0850` 冷启动成功生成 `29.8125s`、`720x720`、`16fps`、带音频视频，已下载本地并上传 R2。
- 修复 KJ runtime 转换：删除 `WanVideoTorchCompileSettings` 节点 `137`，移除 `WanVideoModelLoader.compile_args`，避免 RTX 3090 上 fp8 + TorchInductor 报错；修正 `WanVideoSampler` widget 映射，避免 seed control 导致参数错位。
- 为 KJ 2.0 加入选机安全闸门：默认排除 `CN/TR`，限制 `dph_total<=0.215`，要求 `driver_version>=580`；将 `machine_id=47075 / host_id=74292` 和 `host_id=74292` 加入黑名单，避免再次选择高价且无有效缓存的机器。
- 修复通用 Vast runner 的日志抓取：`vastai logs` 强制使用 UTF-8，避免 Windows GBK 环境遇到特殊字符时只保存编码错误；耗时汇总在缺少 stage marker 时可从 `history.api.json` 反推 prompt 执行耗时。
- 补充 `segmented v4 anchor-overlap` 视觉验收结论：逐帧截图确认 `segv4-anchor-30s-20260430-010327` 后半段不可用，`s02/s03` 原始片段已经出现人物、构图、椅子和动作重置，问题不在 ffmpeg 合并。
- 将 `segmented v4 anchor-overlap` 标记为视觉验收失败：保留脚本和产物用于复盘，但不要作为 30s/60s 生产方案，也不要继续用增加 overlap 或 `xfade/acrossfade` 解决该问题。
- 新增实验入口 `scripts/run_wan_2_2_animate_segmented_v4_anchor_overlap.ps1`：每个分段都用原始 `美女带背景.png` 锚定身份，不再使用 v3 的 `continue_motion` 尾帧续接。
- `segmented v4 anchor-overlap` 支持 `-OverlapSeconds` 默认 `1.0`：后续片段从 `nominal_start - overlap` 切入，下载后优先用 ffmpeg `xfade/acrossfade` 做重叠合并。
- 修复 v4 workflow 输出路径：保留 `WanAnimateToVideo.continue_motion_max_frames=5`，删除非保存用 `VHS_VideoCombine` preview 输出，避免 ComfyUI 只执行 temp 节点 `299`。
- 修复 v4 合并兼容性：`xfade/acrossfade` 结果强制输出 `yuv420p`，避免默认 `yuv444p` 影响浏览器播放。
- 验证 `segmented v4 anchor-overlap` 的 30s 路径：任务 `segv4-anchor-30s-20260430-010327` 在 `1.1-machine-registry` 下命中 `machine_id=56486 / host_id=74292`，补救后 3 段真实 output 全部生成，合并产物 `29.5s`，已上传 R2 并销毁实例。
- 记录 v4 当前状态：虽然 30s 手动补救验证生成了真实输出，且修复后的脚本已通过 `PrepareOnly` 检查，但逐帧视觉验收失败；不能替换 v3 默认入口。
- 更新 `config/version-manifest.json`、`docs/版本管理规范.md`、`skills/wan_2_2_animate_segmented/SKILL.md` 和 `skills/badskills/SKILL.md`，记录 v4 验证结果和不要重复踩的输出节点坑。
- 验证 `segmented v3 single-instance` 的 60s 路径：使用 `素材资产/原视频/光伏60s.mp4` 拆成 6 个 `10s` 片段，在同一个 Vast 实例内依次生成，并用 5 帧尾帧 `continue_motion` 续接。
- 记录验证任务 `segv3-60s-20260429-225542`：本地合并产物 `58.899s`，已上传 R2，实例已销毁并更新机器库。
- 将 `scripts/_test/` 运行日志目录加入 Git 忽略，保持测试日志不进版本控制。
- 验证并整理 `segmented v3 single-instance`：使用 `素材资产/原视频/光伏30s.mp4` 拆成 3 个 `10s` 片段，在同一个 Vast 实例内依次生成，并用 5 帧尾帧 `continue_motion` 续接。
- 将 `wan_2_2_animate_segmented` 的默认入口切到 `scripts/run_wan_2_2_animate_segmented_v3_single_instance.ps1`。
- 记录验证任务 `segv3-fixed-30s-20260429-213538`：本地合并产物 `29.461s`，已上传 R2，实例已销毁并更新机器库。
- 新增版本管理规范和版本矩阵。
- 移除早期 RunComfy Serverless / Node CLI 实验入口，保留当前 Vast 主线需要的 Node 工作流转换脚本。
- 明确区分四类版本：
  - Git release：`v1.0.0`、`v1.1.0`
  - 运行策略：`1.0-cold`、`1.1-machine-registry`
  - 工作流分支：`wan_2_2_animate`、`wan_2_2_animate_segmented`
  - 脚本实现：`segmented v1/v2/v3`
- 纠正历史口径：`v1.1.1` 以前只写在本文档中，但仓库尚无对应 tag，因此不能算正式 release。

## v1.1.0 - Wan2.2 Vast workflow 机器库生产路径

- 新增机器库选择路径。
- 支持 Vast.ai 实例的机器注册表管理。
- 支持 `1.1-machine-registry`：命中历史成功机器时启用 `WarmStart`，未命中则按冷启动处理。
- 优化 Wan2.2 动画工作流的生产环境部署。

## v1.0.0 - Wan2.2 Vast workflow 冷启动基线

- 建立 Wan2.2 Vast 工作流基线版本。
- 支持短视频自动化重写与 AI 视频生成管道。
- 集成 RunComfy 云端推理、OpenAI 转写/重写、Cloudflare R2 存储。
- 支持多种 AI 模型：Wan Animate、LivePortrait、LatentSync、MultiTalk、DreamID 等。
- CLI 提供 `inspect`、`transcribe`、`rewrite`、`pipeline`、`submit` 等完整命令链。
